{{outlet}}
<section>
	<div class="container">
        <div class="row">
            <div class="col-sm-12 padding-right">
				<h2 class="title text-center">Productos en el carrito</h2>
                {{#each model.product_variants as |variant|}}
                    {{#each model.items as |item|}}
                        {{#if (is-equal variant.id item.variant_id) }}
                            <div class="panel panel-default">
                                <div class="panel-heading">
                                    <h2 class="title text-center">{{variant.code}}</h2>
                                </div>
                                <div class="panel-body">
                                    <h4>Descripción:</h4>
                                    <p>{{variant.description}}</p>
                                    <h4>Precio:</h4>
                                    {{#if (eq variant.offerprice null)}}
                                        {{#if (eq item.role "Mayorista")}}
                                            {{variant.wholesaleprice}} Bs.
                                        {{/if}}
                                        {{#if (eq item.role "Especialista")}}
                                            {{variant.specialistprice}} Bs.
                                        {{/if}}
                                        {{#if (eq item.role "Distribuidor")}}
                                            {{variant.importerprice}} Bs.
                                        {{/if}}
                                        {{#if (eq item.role "Minorista")}}
                                            {{variant.price}} Bs.
                                        {{/if}}
                                        {{#if (eq item.role null )}}
                                            {{variant.price}} Bs.
                                        {{/if}}
                                    {{else}}
                                        OFERTA: {{variant.offerprice}} Bs.
                                    {{/if}}
                                    <h4>Cantidad:</h4>
                                    {{item.quantity}}
                                    <button {{action 'addQuantity' item}} type="button" class="btn btn-default">
                                    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span></button>
                                    <button {{action 'restQuantity' item}}type="button" class="btn btn-default">
                                    <span class="glyphicon glyphicon-minus" aria-hidden="true"></span></button>
                                    <button {{action 'deleteToCart' item}} type="button" class="btn btn-danger">
                                    <span class="glyphicon glyphicon-trash"></span> Quitar del carrito</button>
                                </div>
                            </div>
                        {{/if}}
                    {{/each}}
                {{/each}}
            </div>
            {{#if (eq model.items.length 0)}}
                    <br/>
                    <br/>
                    <center>
                        <h2>No se ha agregado productos.</h2>
                        <br/>
                     <a href ="/" type="button" class="btn btn-default">Volver</a>
                    </center>
                    <br/>
                    <br/>
                    <br/>
            {{else}}
            <center>
                <a href ="/" type="button" class="btn btn-default">Seguir Comprando</a>
                <a href="/order" type="button" class="btn btn-info">Continuar con la Compra</a>
            </center>
            <br/>
            {{/if}}
        </div>
    </div>
</section>