{{outlet}}
<section>
    <div class="container">
        <div class="row">
			<!-- Left sideBar -->
			{{left-sidebar categories=model.categories manufacturers=model.manufacturers extras=model.extras}}
			<!-- Left sideBar -->
        <h4>Acerca de nosotros</h4>
        <p class="justified-text text-big">
        {{model.contact.lastObject.about}}
        </p>

        <h4>Misión</h4>
        <p class="justified-text text-big">
            {{model.contact.lastObject.mission}}
        </p>

        <h4>Visión</h4>
        <p class="justified-text text-big">
            {{model.contact.lastObject.vision}}
        </p>
        <img src="{{model.contact.lastObject.picture}}" class="img-responsive" width="870" height="870">
        </div>
    </div>
</section>
